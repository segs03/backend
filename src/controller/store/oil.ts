import { Request, Response } from "express";




export const getOil = (req: Request, res: Response) => {
    const { } = req.body;

    try {

    } catch (err) {
        throw new Error("cannot fetch oil item")
    }
}



