import { Request, Response } from "express";



export const getPastery = (req: Request, res: Response) => {
    const { } = req.body;

    try {

    } catch (err) {
        throw new Error("cannot fetch pastery item")
    }
}



