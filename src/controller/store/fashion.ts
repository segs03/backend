import { Request, Response } from "express";




export const getFashion = (req: Request, res: Response) => {
    const { } = req.body;

    try {

    } catch (err) {
        throw new Error("cannot fetch fashion item")
    }
}



