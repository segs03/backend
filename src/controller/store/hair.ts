import { Request, Response } from "express";




export const getHair = (req: Request, res: Response) => {
    const { } = req.body;

    try {

    } catch (err) {
        throw new Error("cannot fetch hair item")
    }
}



